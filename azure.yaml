name: mssql-fastmcp-server
metadata:
  template: python-container@0.0.1-beta

resourceGroup: rg-AcraSalesAnalytics2

services:
  api:
    project: .
    language: python
    host: containerapp
    docker:
      path: ./Dockerfile
      context: .
